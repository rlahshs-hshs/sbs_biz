# 2026-09-23 (수) SKIP

자막을 확보하지 못했다. 전송하지 않았다.

## 경과 (KST)

- 10:34 루틴 시작. `python3 tg.py check` → `telegram_bot_token: 있음`, `telegram_chat_id: 있음`.
- 10:34 저장소에 `data/transcripts/20260923.txt` 없음 (노트북 `push_transcript.py` 미도착).
- 10:45 ~ 11:35 가이드대로 10분 간격 6회 `git pull --ff-only origin main` → 6회 모두 없음.
- 11:35 직접 수집 1회 시도 (`PYTHONUTF8=1 python3 fetch.py`) → 실패.

## 직접 수집 실패 사유 — YouTube 봇 확인

채널 목록 조회는 됐고 **다시보기는 이미 올라와 있다** (video_id `CW6eqfRnjHc`). "다시보기 미업로드" 경우가 아니다.
막힌 곳은 자막 다운로드 단계로, 어제(9/22 13:35)와 동일한 봇 확인 차단이다.

오류 메시지 앞 300자:

```
yt-dlp 실패:  Only deno is enabled by default; to use another runtime add  --js-runtimes RUNTIME[:PATH]  to your command/config. YouTube extraction without a JS runtime has been deprecated, and some formats may be missing. See  https://github.com/yt-dlp/yt-dlp/wiki/EJS  for details on installing one
WARNING: [youtube] No title found in player responses; f
```

핵심 줄: `ERROR: [youtube] CW6eqfRnjHc: Sign in to confirm you're not a bot. Use --cookies-from-browser or --cookies for the authentication.`

가이드 1단계 지시대로 **다른 우회 방법은 시도하지 않았다** (쿠키·프록시·대체 추출기 모두 안 함).

## 후속 14:04 KST — 해소됨 (이 SKIP 은 무효)

원인은 노트북 스케줄러가 아니라 **인코딩 버그**였다. 노트북에서 `push_transcript.py` 를 `-X utf8` 없이
실행하면(14:00 수동 실행 재현) 자식 yt-dlp 가 cp949 로 쓴 제목을 부모가 utf-8 로 읽어 깨지고,
깨진 제목에 "모닝벨"이 없으니 "다시보기가 아직 채널에 없다"로 오판했다. 노트북 목록 조회 자체는
정상이었다(`CW6eqfRnjHc  2026년 9월 23일 (수) 모닝벨 다시보기` 가 한글로 보임).

- `8cce215` fetch/push_transcript 에 영상 ID 직접 지정 인자 추가 → 노트북에서
  `push_transcript.py 2026-09-23 CW6eqfRnjHc` 로 자막 푸시 성공 (117,982 bytes, 커밋 `196eb21`)
- `59b14b5` `_ytdlp` 자식 프로세스에 PYTHONUTF8/PYTHONIOENCODING 강제. 깨진 json title 도 수정
- 저장소 자막으로 `summaries/20260923.md` 작성 → `python3 tg.py send-file` → `sent`

스케줄러(`pythonw.exe -X utf8`)가 10:05~11:35 에 실제로 돌았는지는 `logs/push_transcript.log` 로 확인 필요.
돌았다면 `-X utf8` 덕에 이 버그는 안 걸렸을 것이므로, 안 올라온 이유는 별개(노트북 절전 등)다.

# 2026-09-22 (화) SKIP

자막 수집 실패 — 2회 시도, 둘 다 실패. 전송하지 않았다.

## 1차 11:45 KST — 네트워크 정책 차단 (해소됨)

실행 환경의 이그레스 프록시가 `www.youtube.com:443` 으로의 CONNECT 를 403 으로 거부했다.
YouTube 봇 확인이 아니라 조직 이그레스 정책 거부이므로 재시도·우회는 하지 않았다
(`/root/.ccr/README.md`: "403 / 407 from the proxy — Do not retry or route around it").
프록시 상태 엔드포인트 기록: `kind: connect_rejected`, `detail: gateway answered 403 to CONNECT (policy denial or upstream failure)`, `host: www.youtube.com:443`.

오류 메시지 앞 300자:

```
yt-dlp 실패: 'Unable to connect to proxy', OSError('Tunnel connection failed: 403 Forbidden')). Retrying (1/3)...
WARNING: [youtube:tab] ('Unable to connect to proxy', OSError('Tunnel connection failed: 403 Forbidden')). Retrying (2/3)...
WARNING: [youtube:tab] ('Unable to connect to proxy', OSError('
```

다시보기 업로드 여부는 확인하지 못했다 (채널 목록 조회 자체가 차단됨).

## 2차 13:35 KST — YouTube 봇 확인 차단

이그레스 차단은 풀렸다. 채널 목록 조회가 정상 동작했고, **다시보기는 이미 올라와 있다**
(video_id `j769BOf9BpQ`). 즉 1단계의 "다시보기 미업로드" 경우가 아니다.

막힌 곳은 자막 다운로드 단계다. yt-dlp 가 봇 확인을 요구했다:

```
WARNING: [youtube] No title found in player responses; falling back to title from initial data. Other metadata may also be missing
ERROR: [youtube] j769BOf9BpQ: Sign in to confirm you're not a bot. Use --cookies-from-browser or --cookies for the authentication.
```

(`fetch.py` 의 `_ytdlp` 이 stderr 끝 800자만 올려주므로 위는 오류 메시지의 뒷부분이다.
같이 나온 부수 경고: yt-dlp 2026.08.19 가 JS 런타임(deno) 없이 도는 중 —
"YouTube extraction without a JS runtime has been deprecated".)

가이드 1단계 지시대로 **다른 우회 방법은 시도하지 않았다** (쿠키 주입, 프록시 우회, 대체 추출기 모두 안 함).
사용자가 판단할 문제다 — 쿠키를 넣어줄지, yt-dlp 를 올리고 JS 런타임을 깔지.

환경변수 `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` 는 이번 실행에 **둘 다 들어오지 않았다**.
자막이 있었더라도 전송은 못 했을 것이다.

## 후속 13:45 KST — 해소됨 (이 SKIP 은 무효)

노트북 쪽에서 `push_transcript.py` 로 자막이 저장소에 올라왔다 (`data/transcripts/20260922.txt`, 117,282 bytes).
저장소 자막을 읽어 `summaries/20260922.md` 를 작성했다. 이 SKIP 기록은 당시 상황 보존용으로만 남긴다.
